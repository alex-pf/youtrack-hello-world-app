# ТЗ: Виджет "Progress Tracking"

## 1. Контекст и цель

"Progress Tracking" — новый дашборд-виджет YouTrack, построенный на основе
существующего виджета "Issues Progress" (`src/widgets/issues-progress/`).
Сохраняет Gantt-визуализацию времени задач в статусах, но:
- добавляет группировку задач по значению поля-справочника (enum custom field),
  при этом **каждая группа рендерится отдельной диаграммой** (не секцией
  внутри одной общей диаграммы);
- заменяет ручные пороги Lead Time (LT50/LT80) на автоматически вычисляемые
  перцентили лид-тайма **для каждой группы отдельно**, по выборке решённых
  задач этой группы;
- сама диаграмма при этом показывает все задачи, попавшие под фильтр
  (resolved и открытые) — ограничение "только resolved" действует **только**
  для расчёта перцентилей фона, не для состава диаграммы.

Основной сценарий использования: сравнение фактического распределения лид-тайма
разных типов/категорий задач (например, Bug vs Feature vs Task) без ручной
настройки нормативов — виджет сам показывает, где на самом деле лежат
50-й и 80-й перцентили каждой группы, закрашивая фон соответствующими зонами.
Отдельные диаграммы на группу выбраны намеренно с расчётом на будущее
усложнение (больше визуальных элементов на диаграмме) — единая диаграмма
на все группы быстро станет нечитаемой.

## 2. Пользовательские сценарии

1. Тимлид добавляет виджет на дашборд, выбирает проект, основной фильтр
   (например `State: Resolved`) и оставляет группировку по умолчанию (Type).
   Виджет строит отдельную Gantt-диаграмму для каждого значения Type
   (Bug/Feature/Task/…), с общим для всех диаграмм шагом сетки по оси X,
   и в фоне каждой диаграммы — зелёная/жёлтая/красная зона по перцентилям
   лид-тайма именно этого типа задач (посчитанным по решённым задачам).
2. Аналитик хочет визуально выделить только задачи с меткой `regression`
   внутри каждого типа: задаёт дополнительный фильтр `tag: regression`.
   Диаграммы показывают все задачи (resolved и открытые), удовлетворяющие
   `основной AND дополнительный`, но зоны фона по-прежнему считаются по
   перцентилям **всех решённых** задач соответствующего типа без
   дополнительного фильтра — чтобы зоны отражали "нормальное" распределение,
   а не искусственно суженную выборку.
3. Пользователь переключает "Группировать по" на другое enum-поле проекта
   (например `Priority`) — набор диаграмм перестраивается с новыми группами
   и новыми перцентилями на группу.
4. Пользователь настраивает шаг сетки по оси X (например "5 дней" вместо
   дефолтного), чтобы диаграммы с большим разбросом лид-тайма были компактнее
   и все диаграммы визуально сопоставимы благодаря единому шагу.

## 3. Настройки виджета

| Поле | Тип | По умолчанию | Описание |
|---|---|---|---|
| `title` | `string \| undefined` | — | Заголовок блока в шапке дашборда. |
| `projects` | `string[]` | `[]` | Проекты — используются для загрузки статусов, enum-полей для группировки; НЕ используются напрямую как фильтр запроса. |
| `primarySearch` | `string` | `''` | Основной фильтр (YouTrack query). Обязателен. Определяет состав диаграмм (вместе с доп. фильтром) И базу для перцентилей (вместе с `groupByField=значение`). |
| `additionalSearch` | `string` | `''` | Дополнительный фильтр. Опционален; влияет только на состав диаграмм, не участвует в расчёте перцентилей. |
| `groupByField` | `string` | `'Type'`, если такое enum-поле есть в проекте(ах); иначе первое найденное enum-поле, иначе пусто | Технический (или локализованный, matched так же как сегодня `Type`) name поля-справочника (**строго enum**-тип), по которому задачи разбиваются на отдельные диаграммы. |
| `statusOrder` | `StatusOrderItem[]` | `[]` | Как в Issues Progress — задаёт стартовый статус (`statusOrder[0]`) для расчёта `leadTimeStartAt`, и порядок/цвет сегментов стека. Общий для всех диаграмм-групп. |
| `showProjectedLT` | `boolean` | `true` | Маркер текущего Estimated Date как точки на оси лид-тайма (`projectedLeadTimeDays`) — как в Issues Progress. Заменяет собой убранную историю Estimate Date (см. раздел 7). |
| `gridStep` | `number` (дней): `1` \| `7` \| `14` \| `28` | `1` | Шаг сетки/тиков по оси X (в днях лид-тайма), **общий для всех диаграмм-групп** — чтобы диаграммы были визуально сопоставимы. Опции в UI: 1 (день), 7 (неделя), 14 (2 недели), 28 (4 недели) — по аналогии с настройкой Grid Step в Issue State History, но там шаг календарный (день/неделя/месяц), а здесь ось — не календарная дата, а "дней от старта". |
| `sortBy` | `'startDate' \| 'issueNumber' \| 'estimatedDate'` | `'startDate'` | Сортировка задач **внутри каждой диаграммы-группы** (не глобально). |
| `refreshInterval` | `number` | `0` | Минуты автообновления; 0 = выключено. |
| `debugMode` | `boolean` | `false` | Показ истории переходов статусов под диаграммами. |
| `description` | `string` | `''` | Markdown-текст под диаграммами. |

Убрано относительно Issues Progress: `search` (заменён на `primarySearch` +
`additionalSearch`), `ltEnabled`, `ltSettings`, `showEstimateDate` (галочка и
история тиков — см. раздел 7).

Сериализация в `StoredWidgetConfig` следует тому же паттерну, что в
`issues-progress/types.ts` (`parseStoredConfig`/`serializeConfig`): булевы
поля как строки `'true'`/`'false'`, массивы/объекты — JSON-строки, `gridStep`
— число (как в `issue-state-history`, где `gridStep` тоже примитив).

## 4. Логика отрисовки диаграмм

- Тип диаграммы: построчные Gantt-полосы, идентичные Issues Progress
  (стек сегментов по `StatusSegment[]`).
- **Каждое значение поля группировки рендерится отдельной диаграммой**
  (отдельный `<svg>`/блок), а не секцией одной общей диаграммы. У каждой
  диаграммы — собственный заголовок (имя группы + число задач), собственная
  Y-ось (список задач группы), но **общий шаг сетки по оси X** (`gridStep`)
  и единый принцип масштабирования, чтобы диаграммы можно было визуально
  сравнивать друг с другом (домен X у каждой диаграммы может отличаться по
  максимуму, но сама сетка — с одинаковым шагом в днях).
- Порядок диаграмм-групп: **по алфавиту** значения поля группировки.
- Внутри каждой диаграммы — сортировка по `sortBy`, применяемая **только к
  задачам этой группы** (не глобально ко всему датасету).
- **Состав диаграммы**: все задачи, удовлетворяющие
  `основной_фильтр AND дополнительный_фильтр AND groupByField = G`, —
  включая нерешённые (открытые). Ограничение "только resolved" относится
  **только** к выборке для расчёта перцентилей фона (раздел 5), не к составу
  самой диаграммы.
- Сегменты, `leadTimeStartAt`, `totalDays`, `projectedLeadTimeDays` —
  вычисляются той же логикой, что в Issues Progress
  (`parseStateSegments`/`findLeadTimeStartAt`/`buildIssueChartData`), без
  изменений в алгоритме, включая открытые задачи (для них `totalDays`/маркер
  считается по текущий момент, как и сегодня в Issues Progress).

## 5. Логика расчёта перцентилей

**Выборка для группы G** (используется только для фона, не для состава
диаграммы): все задачи, удовлетворяющие
`основной_фильтр AND groupByField = G AND resolved != null`
(дополнительный фильтр в эту выборку НЕ входит).

**Лид-тайм задачи** = `(issue.resolved - leadTimeStartAt) / MS_PER_DAY`, где
`leadTimeStartAt` вычисляется через `findLeadTimeStartAt` с тем же
`statusOrder`, что настроен в виджете (тот же алгоритм, что в Issues
Progress: первое вхождение в `statusOrder[0]`, id-или-имя матчинг, fallback на
`issue.created`).

**Формула перцентиля**: линейная интерполяция (эквивалент Excel
`PERCENTILE.INC` / numpy `'linear'`, метод R-7) — подтверждено пользователем.
Для отсортированного по возрастанию массива `days[0..n-1]` и перцентиля `p`
(0–100):
```
index = (p / 100) * (n - 1)
lower = floor(index), upper = ceil(index)
value = days[lower] + (days[upper] - days[lower]) * (index - lower)
```
При `n = 1` — единственное значение используется как и p50, и p80.
При `n = 0` (в группе нет решённых задач) — фон зоны для этой группы не
рисуется (нет данных), но сама диаграмма группы всё равно рендерится, если
в ней есть открытые/нерешённые задачи, попавшие под основной+доп. фильтр.

**Источник данных для расчёта**:
- Если `additionalSearch` пуст — **среди resolved-задач** группы G из уже
  загруженного основного датасета (`основной_фильтр`) выборка эквивалентна
  выборке для перцентилей; отдельный REST-запрос не нужен, перцентили
  считаются из уже загруженных данных (отфильтрованных по `resolved != null`).
- Если `additionalSearch` не пуст — требуется отдельный REST-запрос на
  `основной_фильтр AND groupByField = G` (issues + их активности) для
  каждой группы, представленной в отображаемом датасете, т.к. выборка для
  перцентилей шире, чем то, что отрисовывается.

## 6. Визуальное оформление фоновых зон

- На каждой диаграмме-группе, по всей высоте (все строки задач этой группы),
  рисуются 3 фоновых прямоугольника (позади сегментов задач, за строками):
  - `0 → p50 группы`: зелёный (рекомендуется `#E8F5E9`/полупрозрачный зелёный,
    чтобы не конфликтовать с цветами сегментов статусов).
  - `p50 → p80`: жёлтый (`#FFF8E1`/полупрозрачный).
  - `p80 → maxDomain`: красный (`#FFEBEE`/полупрозрачный).
- Координаты X — по собственной `xScale` данной диаграммы (свой домен на
  каждую группу, т.к. диаграммы отдельные), но с сеткой, привязанной к
  общему `gridStep`. Значения p50/p80 — в днях, свои для каждой группы.
- Полупрозрачность обязательна, чтобы не "забивать" цвета сегментов статусов
  и легенду.

## 7. Что убрано по сравнению с Issues Progress

- Чекбокс "Enable Lead Time thresholds" и связанные per-type поля LT50/LT80
  (`ltEnabled`, `ltSettings`) — полностью удалены из конфигурации и рендера.
  Вместо ручных порогов — автоматические перцентили по группам (раздел 5).
- Единое поле `search` — заменено на `primarySearch` + `additionalSearch`.
- **История Estimate Date** (`showEstimateDate` — галочка "Show Estimate Date
  history" и рендер последовательных тиков-меток изменений даты) — убрана
  полностью, вместе с настройкой. Точка, соответствующая **текущему**
  значению Estimated Date, остаётся — это существующий маркер
  `showProjectedLT` (зелёная метка `projectedLeadTimeDays`), который и так
  сохраняется в новом виджете. Отдельного нового элемента для этого не
  требуется.
- Единый (несекционированный) список задач на одной диаграмме — заменён на
  набор отдельных диаграмм, по одной на группу.
- Глобальная сортировка — заменена на сортировку внутри каждой диаграммы-группы.

## 8. Явные допущения

1. "Поле-справочник" для группировки — **строго enum**-тип
   (`fieldType.valueType` содержит `enum`, с непустым `bundle.values`).
   State/Version/Build/OwnedField/User и прочие bundle-подобные типы полей
   не считаются кандидатами на группировку — подтверждено пользователем.
2. `gridStep` реализуется по образцу `GridStep`-настройки из
   `issue-state-history`, но т.к. ось X здесь измеряется в днях лид-тайма
   (не в календарных датах), значение — число дней между тиками/сеткой,
   а не enum `'day'|'week'|'month'`. Набор опций в UI (`Select`, по аналогии
   с `GRID_STEP_OPTIONS`) — фиксированный, **подтверждено пользователем**:
   `1` (день), `7` (неделя), `14` (2 недели), `28` (4 недели). Значение по
   умолчанию — `1`.
3. Combined-запрос "основной AND дополнительный" реализуется как
   `(${primarySearch}) and (${additionalSearch})` (явный оператор `and`),
   а запрос для перцентилей группы — `(${primarySearch}) and (${groupByField}: {${value}})`
   с YouTrack-синтаксисом `{...}` для значений с пробелами.
4. Диаграммы групп рендерятся вертикально одна под другой в порядке
   алфавита; общий вертикальный скролл виджета, как сегодня в Issues Progress.

## 9. Открытые вопросы

Нет открытых вопросов, требующих решения пользователя перед началом
реализации — все пункты из предыдущей версии ТЗ закрыты. Технические детали
низкого уровня (набор пресетов `gridStep`, точные HEX-цвета зон) оставлены на
усмотрение реализации и будут видны при код-ревью.

## 10. Расширение: этапы (группы статусов) и выбор перцентильной границы

Добавлено после первой версии ТЗ, по итогам работы на реальных данных: в проде у задач
может быть много статусов, и считать перцентили только "от старта до полного resolved"
недостаточно — нужно уметь смотреть LT до промежуточных точек воркфлоу.

### 11.1. Модель данных

`statusOrder: StatusOrderItem[]` (плоский список) заменяется на:

```ts
interface StatusStage {
  id: string;           // сгенерированный на клиенте id (не связан с YouTrack)
  name: string;          // название этапа, задаётся пользователем (например "Analysis")
  statuses: StatusOrderItem[]; // упорядоченный список статусов внутри этапа
}

// WidgetConfig:
statusStages: StatusStage[];   // упорядоченный список этапов
percentileStageId: string;     // id этапа, до которого считать перцентили по умолчанию
```

Это **breaking change** относительно первой версии схемы — старые сохранённые конфиги
с плоским `statusOrder` НЕ мигрируются автоматически (подтверждено пользователем: виджет
ещё не в проде). Пользователь пересохраняет настройки через Configuration UI.

"Плоский" список статусов для алгоритмов, которые его используют как раньше (стек
сегментов, `leadTimeStartAt` = вход в первый статус первого этапа), получается через
`flattenStages(statusStages): StatusOrderItem[]` — конкатенация статусов всех этапов
по порядку. Это единственная точка адаптации для существующего кода сегментов/легенды/
debug-панели — они как принимали `StatusOrderItem[]`, так и принимают, просто на вход
теперь подаётся результат `flattenStages`, а не `config.statusOrder` напрямую.

### 11.2. Пример группировки (из брифа пользователя)

```
1. ToDo:        [ToDo]
2. Analysis:    [Analysis, Ready for In Progress]
3. In Progress: [In Progress, Review, Ready For Test]
4. Test:        [Test]
5. Done:        []
```

Этап может быть пустым (0 статусов) — например, финальный этап-маркер без собственных
статусов, если "Done"-подобный статус уже входит в список нерезолвящих переходов иначе.
Пустой этап просто не участвует в матчинге ни для сегментов, ни для перцентильной
границы (см. 11.3) и не должен предлагаться как валидный выбор в переключателе
перцентилей (см. 11.4) — выбор этапа без статусов не имеет смысла (нечего "покидать").

### 11.3. Перцентильная граница по этапу

Для выбранного этапа `S`, лид-тайм задачи до этого этапа определяется так:

```
LT(issue, S) = (exitAt(issue, S) - leadTimeStartAt(issue)) / MS_PER_DAY
```

где:
- `leadTimeStartAt(issue)` — без изменений, вход в `flattenStages(statusStages)[0]`
  (первый статус первого этапа), как и раньше.
- `exitAt(issue, S)` — момент **последнего** (финального, по времени) выхода задачи
  из любого статуса, входящего в `S.statuses`, до момента `resolved` (подтверждено
  пользователем: если задача уходила из этапа и возвращалась несколько раз — считаем
  по последнему уходу, не по первому достижению). Технически: идём по хронологическому
  timeline статусов (`parseStateTimeline`), обрезанному по `resolved`; находим ПОСЛЕДНИЙ
  по времени entry, чей статус входит в `S.statuses` (матчинг id-или-имя, как в
  `matchesStatus`); `exitAt` — это timestamp СЛЕДУЮЩЕГО entry после него (если такой
  есть в пределах cutoff), иначе — сам `resolved` (задача была в статусах этапа `S`
  вплоть до резолюции).
- Если задача вообще не проходила ни один статус из `S.statuses` (до `resolved`) —
  `exitAt` не определён, задача **исключается** из выборки перцентилей для этого этапа
  (подтверждено пользователем — не берём resolved как fallback, это исказило бы LT
  в меньшую сторону).

Выборка задач для расчёта (кто именно участвует) не меняется — по-прежнему
`основной_фильтр AND groupByField=G AND resolved != null` (раздел 5), просто формула
LT внутри выборки теперь параметризована выбранным этапом `S` вместо жёсткого
"до resolved".

### 11.4. Переключатель этапа на виджете

На самом виджете (не только в настройках) должен быть контрол — Select с списком
непустых этапов (`statusStages.filter(s => s.statuses.length > 0)`) — переключающий,
до какого этапа считать фон-зоны перцентилей. Поведение (подтверждено пользователем,
рекомендованный вариант — "и то, и другое"):
- В Configuration UI задаётся **`percentileStageId` по умолчанию** — сохраняется в
  конфиг при Save, определяет, с каким этапом дашборд открывается впервые/после
  перезагрузки.
- На самом виджете (toolbar, рядом с кнопкой "Обновить") — Select, инициализированный
  значением `config.percentileStageId`, который можно менять "на лету": пересчитывает
  перцентили (новый вызов `fetchGroupPercentiles` с новым `percentileStageId`) и
  перерисовывает фоновые зоны, БЕЗ обращения к Configuration UI и БЕЗ автосохранения
  выбора в конфиг (следующая перезагрузка дашборда снова стартует с сохранённого
  дефолта).

### 11.5. Configuration UI — секция Status Order

Секция "Status Order" в `configuration.tsx` заменяется на редактор этапов:
- Список этапов, каждый — блок с: текстовым полем названия этапа, упорядоченным
  списком статусов внутри этапа (с ↑/↓ и удалением из этапа — статус при удалении
  возвращается в пул "доступных"), кнопками ↑/↓ для порядка самих этапов, кнопкой
  удаления этапа (все его статусы возвращаются в пул "доступных").
- Пул "доступных" статусов — статусы проекта(ов), ещё не добавленные ни в один этап
  (аналог сегодняшнего checkbox-списка `availableStates`, но добавление элемента теперь
  происходит "в выбранный этап", а не в единый список).
- Кнопка "+ Добавить этап" — создаёт новый пустой этап с дефолтным именем
  (например "Этап N"), которое пользователь может переименовать.
- Отдельный Select "Перцентили по умолчанию" — список непустых этапов, значение →
  `percentileStageId`.

### 11.6. Что не меняется

- Алгоритм построения сегментов на диаграмме (`parseStateSegments`) и легенда —
  без изменений, просто на вход подаётся `flattenStages(statusStages)` вместо
  `config.statusOrder`.
- Выборка задач для перцентилей (раздел 5) и способ переиспользования уже загруженных
  данных при пустом `additionalSearch` — без изменений.
- Формула самого перцентиля (линейная интерполяция) — без изменений.

## 11. Разбивка на инкременты

1. **Scaffold widget skeleton** — скопировать `issues-progress/{index.tsx,index.html,widget-icon.svg}` в новый `src/widgets/progress-tracking/`, добавить записи в манифест/vite-конфиг, зарегистрировать заглушку `App`/`widget-settings.json`.
   *Проверка*: виджет появляется в палитре дашборда и монтирует пустой placeholder.

2. **Config data model & fields** — написать `progress-tracking/types.ts` (`WidgetConfig` с `primarySearch`/`additionalSearch`/`groupByField`/`gridStep`, без `ltEnabled`/`ltSettings`/`showEstimateDate`), `parseStoredConfig`/`serializeConfig`; расширить `resources.ts`'s `loadProjectCustomFields`, чтобы также возвращать кандидатов **enum**-полей (строго enum, без state/version/build/ownedField/user).
   *Проверка*: сериализация/парсинг корректно проходят round-trip; поиск enum-полей возвращает верный список на тестовом проекте.

3. **Configuration UI** — собрать `configuration.tsx`: селектор проекта, два `QueryAssist`-фильтра, Select "Группировать по" (по умолчанию Type), Status Order (переиспользуется), Show Projected LT, Select "Шаг сетки" (`gridStep`, по аналогии с Issue State History), Debug, Sort, Refresh, Description; полностью убрать секцию LT-порогов и галочку Show Estimate Date history.
   *Проверка*: сохранение формы даёт `StoredWidgetConfig` новой формы, без LT-полей и без `showEstimateDate`.

4. **Grouping в data pipeline** — адаптировать агрегатор/сортировку в `activity-parser.ts`/`app.tsx`: (а) группировка `IssueChartData[]` **всех** отфильтрованных (`основной AND доп.`) задач по значению `groupByField`, включая открытые, (б) сортировка внутри каждой группы по `sortBy`, (в) отдельно — построение per-group выборки **только resolved** задач для передачи в расчёт перцентилей (раздел 5).
   *Проверка*: на фикстурных данных (смешанные resolved/open, разные значения группы) диаграммы содержат все подходящие задачи (включая открытые), а percentile-выборка содержит только resolved.

5. **Percentile computation service** — реализовать `computePercentile` (линейная интерполяция) и оркестратор группового фетча в `resources.ts` (переиспользование уже загруженных resolved-задач при пустом `additionalSearch`; иначе отдельный REST-запрос `основной AND groupByField=G` на группу).
   *Проверка*: юнит-тесты `computePercentile` на известных массивах; интеграционный тест, что при пустом дополнительном фильтре лишних REST-запросов не происходит.

6. **Gantt chart rendering: отдельные диаграммы на группу + фоновые зоны + общий gridStep** — переписать `gantt-chart.tsx`/обвязку рендера в `app.tsx`: рендер N независимых диаграмм (по одной на группу, в алфавитном порядке), каждая со своим доменом X, но общей величиной шага сетки (`gridStep`); фоновые прямоугольники (зелёный/жёлтый/красный) по `{p50,p80}` своей группы; убрать весь код LT-маркеров и рендер истории Estimate Date, сохранить рендер маркера `showProjectedLT`.
   *Проверка*: визуальная/ручная проверка на фикстурном датасете с 2+ группами — каждая группа на своей диаграмме, зоны соответствуют перцентилям своей группы, шаг сетки визуально одинаков на всех диаграммах, нет остатков LT50/LT80 и Estimate Date history UI.

7. **App wiring & end-to-end data flow** — `app.tsx`: оркестрация основного фетча (`primarySearch AND additionalSearch`), группировки, группового percentile-фетча/переиспользования, передача данных в набор `GanttChart`-инстансов; сохранить debug-панель, description, автообновление, projected-LT без изменений.
   *Проверка*: полная загрузка виджета на реальном/тестовом YouTrack-проекте даёт корректный набор диаграмм с видимыми зонами, верным заголовком/счётчиком и единым шагом сетки.

8. **Docs & manifest polish** — написать `progress-tracking/WIDGET_DOCS.md` (по структуре `issues-progress/WIDGET_DOCS.md`), финализировать схему `widget-settings.json` под все хранимые поля (включая `gridStep`), обновить версию в `manifest.json`.
   *Проверка*: схема совпадает с полями `serializeConfig` один в один; документация сверена с фактическим поведением кода.

9. **Этапы: модель данных + логика границы** (см. раздел 10) — заменить `statusOrder: StatusOrderItem[]` на `statusStages: StatusStage[]` + `percentileStageId: string` в `types.ts` (breaking change, без миграции); добавить `flattenStages()`; добавить в `activity-parser.ts` `findStageExitAt(activities, statusStages, stageId, issueCreatedAt, issueResolvedAt)`; адаптировать `percentiles.ts` — `stageLeadTimeDaysForResolvedIssue` вместо `leadTimeDaysForResolvedIssue`, `fetchGroupPercentiles` принимает `statusStages`+`percentileStageId` вместо `statusOrder`.
   *Проверка*: юнит-проверка `findStageExitAt` на фикстурном timeline с возвратом в предыдущий этап — возвращает момент ПОСЛЕДНЕГО выхода; задача, не достигшая этапа, даёт `null`.

10. **Configuration UI: редактор этапов** — переписать секцию "Status Order" в `configuration.tsx` на многоэтапный редактор (список этапов с названием и упорядоченными статусами внутри, пул доступных статусов, ↑/↓ для этапов и для статусов внутри этапа, добавление/удаление этапа); добавить Select "Перцентили по умолчанию" (`percentileStageId`, только непустые этапы).
    *Проверка*: создание 2+ этапов, перемещение статусов между ними, сохранение даёт `StoredWidgetConfig.statusStages`/`percentileStageId` ожидаемой формы.

11. **App wiring: живой переключатель этапа** — `app.tsx`: `flattenStages(config.statusStages)` вместо `config.statusOrder` везде, где он использовался (сегменты, `leadTimeStartAt`, debug-панель); toolbar-Select со списком непустых этапов, инициализированный `config.percentileStageId`, меняющий выбранный этап локально (без записи в конфиг) и триггерящий пересчёт `fetchGroupPercentiles` с новым `percentileStageId`.
    *Проверка*: переключение этапа в toolbar пересчитывает и перерисовывает фоновые зоны без захода в настройки; перезагрузка виджета стартует с `percentileStageId` из сохранённого конфига.

12. **Docs polish (этапы)** — обновить `progress-tracking/WIDGET_DOCS.md` и `widget-settings.json` под новую схему конфига (`statusStages`, `percentileStageId` вместо `statusOrder`).
    *Проверка*: схема `widget-settings.json` совпадает с `serializeConfig` один в один; `WIDGET_DOCS.md` описывает фактическое поведение переключателя этапов.

### Ключевые файлы для реализации
- src/widgets/issues-progress/types.ts
- src/widgets/issues-progress/activity-parser.ts
- src/widgets/issues-progress/resources.ts
- src/widgets/issues-progress/gantt-chart.tsx
- src/widgets/issues-progress/configuration.tsx
- src/widgets/issue-state-history/types.ts (образец `GridStep`)
- src/widgets/issue-state-history/gantt-chart.tsx (образец рендера сетки по `gridStep`)
- manifest.json
- vite.config.ts
